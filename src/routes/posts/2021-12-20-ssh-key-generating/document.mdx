import "./document.css";

<div className="document">
Последнее время мне часто приходиться генерировать ssh ключи, переустановил ОС на своем ПК, выдали новый ноутбук на работе или просто помощь коллеге. И я постоянно забываю этот стандартный набор комманд для генерации ssh ключа, так вот в этом "копипасте" я постараюсь вкратце описать все этапы и детали создания ssh ключа🔑

- ### Шаг 1

  `ssh-keygen -t ed25519 -C "your_email@example.com"` здесь мы пишем почту к которой привязан наш GitHub аккаунт, и нажимаем Enter

- ### Шаг 2

  Далее вам нужно будет выбрать название файла где будет хранится сам ключ. Вы можете оставить как есть в данном случае(id_ed25519), или же назвать как вам удобно. Например: mySshKey, super_ssh...
  `> Enter a file in which to save the key (/c/Users/you/.ssh/id_ed25519):[Press enter]`
  И после того как вы определились с названием просто нажмите Enter.

- ### Шаг 3

  Теперь вам нужно ввести пароль к вашему ssh ключу, его вы будете воодить во время клонирования репозиториев через shh, во время git push и тд...
  Советую написать пин-код, либо короткий, но надежный пароль, так как вам придется часто писать его.
  `

  > Enter passphrase (empty for no passphrase): [Type a passphrase]
  > Enter same passphrase again: [Type passphrase again]
  > `

  - ### Шаг 4
    Далее нам нужно активировать ssh agent
    `eval "$(ssh-agent -s)"`
    После этого вы увидите примерно такой ответ
    `> Agent pid 59566` число будет другое, это совсем не страшно😁👌

  - ### Шаг 4
    Далее нам нужно активировать ssh agent
    `eval "$(ssh-agent -s)"`


```json

```

</div>
```
