import "./document.css";

<div className="document">

Создаем файл `jsconfig.json` если его нет, если есть, то просто вставляем туда следующий кусок кода

Если вы испольузете TypeScript в вашем проекте то у вас файл будет называться `tsconfig.json`

Файл `jsconfig.json` или `tsconfig.json` должен находится в корне вашего проекта!

```json
{
    "compilerOptions": {
        "baseUrl": "src"
    }
}
```

</div>
