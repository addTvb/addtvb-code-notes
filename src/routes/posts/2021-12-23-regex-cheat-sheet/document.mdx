import "./document.css";

<div className="document">

Ну что попытаюсь просто показать как пользоваться регулярками(regex)😁

- Для того чтобы проверить строку на наличие какого то определенного слова вы просто пишите это слово между двумя "слэш"(/)
```js
	let myString = "Hello World";
	let myRegex = /Hello/;
	let result = myRegex.test(myString); // true!
```

- Чтобы проверить наличие одного из нескольких слов то просто разделите их с помощью логического ИЛИ(|), если хотя бы одно из нескольких слов будет в вашей строке то ответ будет true
```js
	let myString = "You go to holiday every summer";
	let myRegex = /go|holiday|winter|summer/;
	let result = myRegex.test(myString); // true!
```

- Для игнорирования регистра существует "флаг" i, просто добавьте его после закрывающегося "слеш" и ваша регулярка не будет обращать снимание на регистр.
```js
	let myString = "addTvb";
	let myRegex = /addtvb/i;
	let result = myRegex.test(myString); // true!
```

Продолжение следует...

</div>
